export type MediaHandler = {
  isDesktop: boolean,
  isMobile: boolean,
}
export type Service = {
  title: string,
  desc: string,
  cta: string,
  icon: string,
  url: string,
  isPopular: boolean,
}
