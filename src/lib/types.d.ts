export type MediaHandler = {
  isDesktop: boolean,
  isMobile: boolean,
}
export type Service = {
  title: string,
  desc: string,
  cta: string,
  icon: string,
  url: string,
  isPopular: boolean,
}
export type NavData = {
  name: string,
  url: string
}
export type ProdSummary = {
  name: string,
  url: string,
  imgUrl: string,
  country: string,
  min_price: number,
}
