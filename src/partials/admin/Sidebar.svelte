<script>
  import { page } from '$app/stores';
	import Logo from '$lib/Components/Logo.svelte';
	import { homeSVG, productSVG, eSimSVG, gamesSVGAlt, giftCardSVGAlt, topUpSVGAlt, usersSVG } from '$lib/Components/iconPaths';

  /** @type {Object<string, string>}*/
  let icons = {
    homeSVG,
    productSVG,
    eSimSVG,
    gamesSVGAlt,
    giftCardSVGAlt,
    topUpSVGAlt,
    usersSVG,
  }

    /** @type {import('$lib/types').AdminNavMenuItem[]} */
  export let admin_routes = [];
</script>

<div id="hs-application-sidebar" class="hs-overlay  [--auto-close:lg] hs-overlay-open:translate-x-0 -translate-x-full transition-all duration-300 transform w-[260px] h-full hidden fixed inset-y-0 start-0 z-[60] bg-white border-e border-gray-200 lg:block lg:translate-x-0 lg:end-auto lg:bottom-0 dark:bg-neutral-800 dark:border-neutral-700" role="dialog" tabindex="-1" aria-label="Sidebar">
  <div class="relative flex flex-col h-full max-h-full">
    <div class="px-6 pt-4">
      <Logo />
    </div>

    <div class="h-full overflow-y-auto [&::-webkit-scrollbar]:w-2 [&::-webkit-scrollbar-thumb]:rounded-full [&::-webkit-scrollbar-track]:bg-gray-100 [&::-webkit-scrollbar-thumb]:bg-gray-300 dark:[&::-webkit-scrollbar-track]:bg-neutral-700 dark:[&::-webkit-scrollbar-thumb]:bg-neutral-500 mt-5">
      <nav class="hs-accordion-group p-3 w-full flex flex-col flex-wrap" data-hs-accordion-always-open>
        <ul class="flex flex-col space-y-1">
          {#each admin_routes as route}
          <li>
            <a class="flex items-center gap-x-3.5 py-2 px-2.5 text-sm text-gray-800 rounded-lg focus:outline-none dark:text-white" href={route.uri} class:bg-gray-100={$page.url.pathname == route.uri} class:bg-neutral-700={$page.url.pathname == route.uri}  class:text-white={$page.url.pathname == route.uri}>
              <span class="max-w-4">{@html icons[route.icon]}</span>
              {route.name}
            </a>
          </li>
          {/each}
        </ul>
      </nav>
    </div>
  </div>
</div>
